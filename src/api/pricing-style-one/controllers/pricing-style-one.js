'use strict';

/**
 * pricing-style-one controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::pricing-style-one.pricing-style-one');