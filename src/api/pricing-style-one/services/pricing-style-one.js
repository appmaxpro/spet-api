'use strict';

/**
 * pricing-style-one service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::pricing-style-one.pricing-style-one');